version: 2.1
orbs:
  docker: circleci/aws-ecr@6.7.0
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - docker/build:
          image: vaultwarden/server 

workflows:
  example-workflow:
    jobs:
      - example-job
